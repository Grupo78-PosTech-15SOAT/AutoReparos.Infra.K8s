apiVersion: v1
kind: Service
metadata:
  name: autoreparos-loki
  labels:
    app: autoreparos-loki
    {{- include "observability.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: autoreparos-loki
  ports:
    - port: 3100
      targetPort: 3100
